# M1 实施计划 — 基座:Fork、裁剪、重品牌、Landing(2026-07-21)

> 开工任何分片前:重读 `WORKORDER.md` 相关章节 + 本文件 + `docs/logs/M1-LOG.md`。
> 每分片完成定义:功能跑通 + 构建无错 + LOG 追加 + git commit & push。

## 事实依据(2026-07-21 实地核查上游仓库)

- monkeytype 前端已迁移到 **SolidJS + TypeScript + Vite 8 + Tailwind 4 + Chart.js**(不再是 vanilla TS);TanStack 全家桶、anime.js
- 仓库为 **pnpm(10.28.1)+ turbo monorepo**:`frontend/` `backend/` `packages/`;Node 要求 **24.11.0**(`.nvmrc`)
- **前端可独立运行**:根目录 `pnpm i` → `npm run dev-fe` → localhost:3000;`frontend/src/ts/constants/firebase-config-example.ts` 复制为 `firebase-config.ts` 且字段留空即可 → 账号功能自动禁用 = 天然的游客模式,M1 不需要任何后端/数据库/Firebase
- 主题与字体体系见上游 `docs/THEMES.md`、`docs/FONTS.md`;上游自带 `CLAUDE.md`/`AGENTS.md` 和 `docs/`
- GitHub 账号 26ds 已授权,fork 到该账号名下

## 分片

### M1a — Fork + 环境跑通 + 代码地图

- Fork `monkeytypegame/monkeytype` 到 26ds 名下并把仓库改名为 `typeany`(保留 fork 关系 = 源码公开,满足 GPL;README 后续在 M1c 声明)
- Clone 到 `打字项目/typeany/`
- 迁入本目录的 `WORKORDER.md`、`CLAUDE.md`、`docs/plans`、`docs/logs`、`docs/UI-brief.md`、`design/` 到仓库根:我们的 CLAUDE.md 为准(上游 CLAUDE.md/AGENTS.md 内容并为其中一节"上游开发说明"或保留原位仅作参考);我们的计划/日志并入其 `docs/` 下,无命名冲突
- 环境:`nvm install 24.11.0` + `npm i -g pnpm@10.28.1` + `pnpm i`;复制 firebase-config 留空
- `npm run dev-fe` 跑通;通读 `frontend/src`,把**代码结构地图**写进 LOG:入口/路由、打字引擎(判定、caret、wpm 计算)、config 体系、主题体系、顶栏/页脚组件、广告与 Sentry/统计代码位置
- **验证**:localhost:3000 完整打一轮英文测试、出结算曲线;首个 commit 推上 GitHub
- 风险:Node 24 与 pnpm 版本;依赖安装体量;Sentry/analytics 无配置时的报错(记录下来,M1b 处理)

### M1b — 界面裁剪

- 移除:`quote`、`zen` 模式按钮;顶栏皇冠(排行榜)、info、通知铃铛;广告位;Sentry/第三方统计;捐赠/社交入口
- 保留:`punctuation` `numbers` `time` `words` `custom`、字数档 10/25/50/100+自定义、键盘图标、设置、账号图标
- 策略:**隐藏入口 + 禁用路由优先,不深删模块**(避免连锁破坏;深删随 M2/M3 顺手做);lint/build 保持通过
- **验证**:界面元素对照 WORKORDER.md 裁剪表逐项核对;打字与结算无回归;build 无错

### M1c — 重品牌 + Ink Aurora 基础主题

- 清除 Monkeytype 品牌:页面 title/meta/OG、PWA manifest、footer、社交链接;临时文字 logo `TypeAny`(Sora 字体);favicon 占位
- 新建 `typeany` 主题并设为默认:`design/README.md` B 方案色板映射到 monkeytype 主题变量;玻璃拟态 v1(页面背景渐变 + 主面板半透明 + blur,参数照 design 文件);字体引入 Sora / IBM Plex Mono(中文先用系统兜底,字体文件 M5 处理)
- GPL 合规:保留 LICENSE 与版权声明;重写 README:声明 fork 自 monkeytype、GPL-3.0、链接上游仓库
- **验证**:全局搜索无 "monkeytype" 残留(LICENSE/README 致谢除外);默认主题生效;正文对比度抽查过 AA;build 无错

### M1d — Landing 双入口 + 游客路由

- 新增 landing(路由 `/`):Sora 大字标 + 玻璃按钮 `Upload & Type` / `Random Mode`,动效按 design 动效基线(hover 亮度+上移,prefers-reduced-motion 降级)
- `Random Mode` → 打字页(游客直接玩,成绩存本地);`Upload & Type` → 书架占位页(提示:上传 M2/M4、登录 M6)
- 打字页路由调整(如 `/` = landing、`/test` = 打字;落地时按上游 router 实际结构定,记入 LOG)
- **验证**:双入口可达、刷新不 404、游客完整打一轮 + 结算曲线正常

## M1 整体完成定义

dev-fe 与 build 全绿;GitHub `26ds/typeany` 含全部代码 + docs + design;`docs/logs/M1-LOG.md` 四条齐;换一台电脑 clone 后按 CLAUDE.md 可无缝继续。

## 明确不做(留给后续)

custom 弹窗内部裁剪(M2)、书架真实功能与上传(M2/M4)、书籍层(M3)、中文(M5)、登录与云端(M6)、玻璃拟态逐页细化(跟随 UI 线框迭代)。
